# Firebase Backend for Asset Repair Monitoring System

This is the Firebase Cloud Functions backend for the full stack asset repair monitoring system.

## Setup
1. Install Firebase CLI:
   ```bash
   npm install -g firebase-tools
